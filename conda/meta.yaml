requirements:
  host:
    - python
    - pip
  run:
    - python
    - mtsv-tools >=2.1.0
    - click
    - biopython
    - pysam
    - intervaltree
    - ete3
    - tqdm
    - tabix
    - bgzip
test:
  requires:
    - pytest
run_constrained:
  - mtsv-tools >=2.1.0
